
- name: loops
  hosts: localhost  # Corrected from 'localhosts' to 'localhost'
  tasks:  # Corrected typo from 'taks' to 'tasks'
  - name: print persons
    ansible.builtin.debug:
      msg: "Hello {{ item }}"  # Ensures proper variable interpolation
    loop:
      - ramesh
      - suresh  # Fixed indentation issue and removed erroneous dash
      - robert
      - raheem


